"""Scene graph dataclass (deprecated — not used in the pipeline).

The symbolic pipeline passes raw tensors directly from SceneParser to Executor.
This abstraction was planned but not integrated.
"""

if __name__ == "__main__":
    print("SceneGraph module is deprecated — no active code to test.")
